# Compatibility entry point: requires --artifacts and --repository.
from pathlib import Path
import subprocess
import sys
import __main__
root = Path(__file__).resolve().parent
subprocess.run([sys.executable, str(root / "tools/publish_release.py"), *getattr(__main__, "unknown_args", [])], check=True)
